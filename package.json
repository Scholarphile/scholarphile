{
  "name": "scholarphile",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -R index.html style.css script.js dist/",
    "start": "python3 -m http.server 8080"
  }
}
