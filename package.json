{
  "name": "scholarphile",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -R *.html *.css *.js _redirects dist/",
    "start": "python3 -m http.server 8080"
  }
}
